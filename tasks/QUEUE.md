# Task queue — maintained by the coordinator (chat Claude)

Sessions: `git pull`, claim in docs/LANES.md, execute, log, push, re-read this
file. **Do not edit this file** — the coordinator owns it.

## STANDING RULES
Batches under $20 auto-submit; wave-then-checkpoint; his role is only spot
checks, GUI review, account-gated fetching. Canonical-over-fork for repos.

## STRATEGY (human ruling 2026-08-25)
Tier 2 goes DEEP, in order: (A) exhaustively find ALL own-group repos first —
most MEng/PhD theses should have one, and more papers do too; (B) rank own
repos by stars x paper citations; coordinator picks the outside-user hunt
list from that ranking; (C) those hunts tie into a broadened tier 3.
Do A before B; B's report STOPs for coordinator gating.

## OPEN — round 7 (2026-08-25)
1. [sonnet] **own-repo-deep-hunt, discovery**: goal = a confirmed own-group
   repo (or confirmed absence) for every paper, esp. the ~108 theses/TRs.
   Steps: (a) enumerate ALL group GitHub orgs (BuildIt-lang, GraphIt-DSL,
   exaloop, mit-commit, CommitCS?, plus any found via members of known
   repos) and list every repo with dates/topics; (b) build an author→GitHub
   -account map: ORCID records, personal homepages, and commit-author
   emails/handles mined from the repos we already verified; (c) for each
   account, enumerate public repos created in that author's publication
   window; (d) candidate-match repos to papers (title/abstract/README/
   dates), folding in the 165 medium rows from
   harvest/repos/candidates.json. Output harvest/repos/deephunt.json
   with per-paper candidates + evidence. Mechanical steps direct; the
   match JUDGMENT goes through a Batch curation (under-$20 rule) with
   evidence per row. Merge confirmed rows into verified.json (role
   own_group), log counts: papers newly covered / still repo-less.
2. [sonnet] **own-repo ranking (after 1)**: for every own-group repo, a
   ranking sheet — stars, forks, last-commit, dependents if cheap, x the
   anchor paper's citation count → harvest/ecosystems/ranking.md sorted
   by hunt-worthiness. STOP: coordinator picks the tier-2 user-hunt list
   (dynamorio needs explicit human approval — potentially huge).
3. [sonnet] **login-worklist2** (may already be claimed in a tab): build
   harvest/fulltext/login-worklist2.json — all low/medium-confidence
   contexts-only detailed-side or title-only-with-DOI rows, IEEE/ACM/
   Springer (skip Elsevier), IEEE arnumbers via batched Crossref;
   ready-to-fetch URLs + doi-derived names; report rows per publisher.
4. [sonnet] **verified-quirks** (carried): duplicate URL spellings +
   own-group rows misroled third_party in harvest/repos/verified.json;
   rebuild affected data/ outputs; impact-view drops its client-side
   filters after.
5. [sonnet] **ecosystems measure-step** (carried): the 3 pilot tiers are
   live, but harvest/ecosystems/candidates-report.md is still absent from
   main — commit it if local, else run the MEASURE step (ecosystem-size
   estimate per own-group repo) and STOP for gating. Task 2's ranking
   supersedes parts of this; merge rather than duplicate.
6. [sonnet] **halide-import — verify** (carried): confirm tier-2/3 rows
   for halide:pldi:2013 are sourced from samanamarasinghe/Halide-world
   with attribution; if not, import per the round-4 spec.

## PARKED (needs the human)
- Login fetch sitting #2: coordinator generates runners once task 3 lands.
- Authors sitting #2: harvest/authors/session-sheet.md is ready.
- Tier-2 user-hunt list: gated on task 2 (coordinator), dynamorio on him.

## DONE (recent)
- Login rejudge: 23/23 ingested, flip rate 94% pilot / 54% non-pilot →
  larger login fetch approved.
- Descendants: harvest/repos/descendants.json all papers; widened-bucket
  search running in a peer tab.
- ALL summaries live; impact view live (131 papers with repo panels, 162
  repos + 23 artifacts); citation feature complete.
