# Task queue — maintained by the coordinator (chat Claude)

Sessions: `git pull`, claim in docs/LANES.md, execute, log, push, re-read this
file. **Do not edit this file** — the coordinator owns it.

## FULL-FORCE ROLLOUT (approved 2026-08-25)
Pilot phase is closed: taxonomy, citation view, merged summaries, impact view
all approved with his feedback. Scale to ALL papers. Standing rules: batches
under $20 auto-submit; wave-then-checkpoint, never one giant pass; his role is
only spot checks, GUI review, and account-gated fetching.

## FABLE TASKS
F1. **summaries-at-scale.** FIRST: write docs/summary-style.md capturing the
    approved voice — the 9 pilot summaries as exemplars plus every point of
    his critique feedback (get it out of session memory into the repo). Then
    build the generation pipeline: per paper an evidence pack (existing
    summary text, classification stats, top engaged citers, verified repos,
    artifact, notable descendants) + the style doc -> Batch API generation of
    the reception part, written to the separate reception field per the
    merge ruling (NEVER overwrite hand-written summary text; the seam edit
    happens in the rendered flow rules already established). Run in WAVES of
    ~25 papers, review each wave yourself against the style doc and hand-fix
    weak ones before pushing. **After wave 1, STOP — the human spot-checks
    ~10 before waves 2+ run.** Order waves by citation count descending.
F2. **impact-view rollout** (after or interleaved with F1): apply the
    approved impact view (with his feedback) to ALL papers; repos render
    wherever tier data exists, gracefully absent elsewhere. Keep the TEMP
    pilot button until cutover. Report: papers rendered, initial-load payload
    unchanged.

## SONNET TASKS
S1. **ecosystems**: finish taco/codon/streamit (in progress). Then MEASURE
    before harvesting further: for every own-group verified repo, report
    stars/forks/dependents as an ecosystem-size estimate to
    harvest/ecosystems/candidates-report.md and STOP — the coordinator picks
    which ecosystems get tier-2 harvests (dynamorio in particular may be
    huge; do not start it unapproved).
S2. **descendants-all**: extend tier-3 idea-descendant extraction from the 9
    pilots to every paper (mechanical over the classification data; same
    no-guessing rule; paper-only when no repo located).
S3. **login-worklist**: build the closed-citing-papers worklist for the
    human's browser sitting — rows classified detailed-side with confidence
    low whose evidence is contexts-only and whose DOI prefix is IEEE/ACM/
    Springer/Elsevier. Group by publisher, emit
    harvest/fulltext/login-worklist.json + a one-page run sheet. Do NOT
    fetch anything.
S4. **authors-worklist**: prepare the human sitting for authors — the 77
    unresolved + 5 ambiguous with best-guess links, plus the LinkedIn
    presence checklist for all 369, as harvest/authors/session-sheet.md.
    Do NOT visit LinkedIn.
S5. **refresh-docs**: write docs/refresh.md — the every-few-months procedure
    (new papers in, ID map, harvests, classification, GS counts, summaries,
    rebuild), each step naming its script and who runs it (worker vs human).

## DONE (recent)
- repo-verify all 303 papers; impact-view design approved; pilot summaries
  approved with feedback; GS counts merged; corpus classified (11,082)
