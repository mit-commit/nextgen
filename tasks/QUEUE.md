# Task queue — maintained by the coordinator (chat Claude)

Sessions: `git pull`, claim in docs/LANES.md, execute, log, push, re-read this
file. **Do not edit this file** — the coordinator owns it.

## STANDING RULES
Batches under $20 auto-submit; wave-then-checkpoint; his role is only spot
checks, GUI review, account-gated fetching. Canonical-over-fork for repos.

## HUMAN RULINGS 2026-08-25 (round 8)
Tier-2: hunt ALL 52 ecosystems including dynamorio (the ~$8 verify is under
the auto-submit line). Halide's 567 staged tier-2 rows: FOLD into the live
site. deephunt_review.json: trust the audit trail — no human spot-check, but
DO improve confidence by machine means where cheap.

## OPEN — round 8 (2026-08-25)
1. [sonnet] **own-inventory fold + repos rebuild**: the deferred merge both
   deep-hunt sessions left — fold harvest/repos/own-inventory.json's
   confirmed rows into harvest/repos/verified.json (canonical-over-fork,
   dedupe by numeric repo id per dedupe_verified_repos.py), THEN rerun
   build_repo_data.py + gen_tier2_priority.py so the 26 papers carrying
   bare owner/repo urls (fixed in verify_deephunt.py) rebuild with real
   URLs and GitHub enrichment. Report new data/repos counts.
2. [sonnet] **tier-2 hunt, ALL ecosystems**: run the verification batch
   over the 810 candidates from the measure step (all 52 own repos,
   dynamorio included — human-approved). Same integration vocabulary and
   evidence standard as the 3 pilots. Merge confirmed outside-user rows
   into data/repos/ per SCHEMA.md (respect site-citations' path claims —
   coordinate via LANES.md as before). Report per-ecosystem confirmed
   counts.
3. [sonnet] **halide tier-2 fold** (human-approved): fold the 567 staged
   rows in harvest/ecosystems/ (from build_halide_import.py) into
   data/repos/ for halide:pldi:2013 — mapped, never re-judged, attributed
   to samanamarasinghe/Halide-world; third_party_bundle/prose_only stay
   excluded. Rebuild affected data/repos files.
3b. [fable, after 2+3 land] **impact-view spot-render**: load the site
   locally, exercise the biggest new panels (halide, dynamorio, taco),
   confirm tier bars/sorts/counts still behave with the larger tier-2
   population; fix display-only issues, flag anything structural.
4. [sonnet] **deephunt confidence pass**: for the 12 rows in
   harvest/repos/deephunt_review.json, gather FULLER evidence (repo
   README + file listing + the paper's abstract/title) and re-judge each
   with an independent batch call at high reasoning effort. Accept a row
   only when the new judgment AGREES with the original accept; demote
   disagreements to rejected with the reason logged. Merge accepts per
   task 1's route.
5. [sonnet] **widened descendants search wave** (carried if not claimed):
   the uses-tool/core widened bucket (1,060 rows) has only the mechanical
   scan — run the search+verify step (same pattern as the strong wave,
   expect a high FP rate at verify; that is the design working). Fold
   located repos into descendants.json; site fold follows the established
   route.

## PARKED (needs the human)
- Login fetch sitting #2: RUNNING on his machine (3,641 rows; IEEE needs
  periodic re-login). When staging fills, coordinator queues the
  ingest+rejudge round.
- Authors sitting #2: harvest/authors/session-sheet.md is ready.
- Cutover: TEMP pilot button deletion + final review, on his word.

## DONE (recent)
- Round 7 complete: deep hunt (+39 own repos, 157 papers covered),
  tier2-priority.md (71 ranked), ecosystem measure (52 sized, 810
  candidates, ~$8), halide-import (tier-3 live, tier-2 staged),
  verified-quirks dedupe, worklist2 (3,641 rows), descendants widened
  wave (47 repos live), bare-URL bug fixed at source.
