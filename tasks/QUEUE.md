# Task queue — maintained by the coordinator (chat Claude)

Sessions: `git pull`, pick the first OPEN task whose paths are unclaimed in
docs/LANES.md, claim it there, do it, log completion in docs/LANES.md, push,
then re-read this file for the next task. **Do not edit this file** — the
coordinator owns it.

## HUMAN RULINGS IN FORCE (2026-08-24)
- Taxonomy v0.2 + citation view (3 sort modes, header toggle) APPROVED.
- Human involvement is ONLY: account-gated scraping, GUI evaluation, spot
  checks. Everything else is decided by the coordinator/sessions; surface
  only decisions he might overrule.
- Follow-up classification batches under $20 may submit WITHOUT asking;
  above that, stop for approval.
- Dedup: fold same-work records by normalized title, keep highest-evidence
  sibling, before public counts.

## OPEN
1. [sonnet] **repos-search step 2** (claims harvest/repos/): per
   harvest/repos/search-plan.json, GitHub code/repo search on each of the 268
   papers' keyword sets (GITHUB_TOKEN in env). Score candidates (name matches
   software, owner matches author/org, README mentions title/authors, created
   within ~2y of publication). Auto-accept NOTHING: top-3 candidates with
   evidence + confidence to harvest/repos/candidates.json. Report: strong /
   weak-only / none counts.
2. [sonnet] **abstracts-all + rejudge sweep** (claims harvest/fulltext/
   abstracts*, coordinates with taxonomy lane's claims for records): extend
   the OpenAlex abstract harvest to EVERY citing work across
   harvest/citations/*.json that lacks cached evidence (currently abstracts
   exist for the 8 pilots only). Then re-classify via the existing
   curate/classify_citations.py exactly the rows whose function is
   unknown/unclassified or confidence low AND which gained an abstract —
   batch submit directly if the estimate is under $20, else stop and report.
   Merge results per SCHEMA.md.
3. [sonnet] **cited_by verify**: confirm every citing work in
   data/citations/*.json carries `cited_by` per SCHEMA.md (popularity sort
   needs it); backfill gaps from the stored OpenAlex records first, API only
   for what's missing. Skip if the merge already did this — verify, then log.
4. [sonnet] **site-integration** (claims publications.html + assets for the
   citation view in THIS repo only): wire the approved citation view into
   nextgen's publications.html for all 144 papers with data/citations files —
   lazy-load per paper on expand, three sort modes + header toggle, exactly
   the approved prototype behavior. Keep prototype/ untouched for reference.
   Report: papers wired, payload added to initial page load (must be ~0).

## DONE (this round)
- classify-corpus COMPLETE: 11,082/11,082 citing works classified, merged
  into data/citations/ (144 papers + 8 pilots = full corpus)
- citations lane: 177 files, 26,192 citing works, S2-enriched
- idmap review EMPTY (all 327 accounted)
- artifacts review EMPTY (23 found, 20 with ACM badges)
- authors: 287/369 OpenAlex-resolved; 77 unresolved + 5 ambiguous parked for
  a later browser session with the human
