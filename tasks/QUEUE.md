# Task queue — maintained by the coordinator (chat Claude)

Sessions: `git pull`, claim in docs/LANES.md, execute, log, push, re-read this
file. **Do not edit this file** — the coordinator owns it.

## STANDING RULES
Batches under $20 auto-submit; wave-then-checkpoint; his role is only spot
checks, GUI review, account-gated fetching. Canonical-over-fork for repos.

## OPEN — round 6 (2026-08-25)
1. [sonnet] **login-pdf-ingest** (claims harvest/fulltext/ for these rows):
   ~/workspace/nextgen-fulltext holds 23 publisher PDFs fetched via his
   login (+ _run-log.json). Extract into the fulltext cache (pypdf,
   surrogate-safe write, >2,000-char floor), re-judge exactly those
   citing-work rows with full text as evidence, merge upgraded
   classifications per SCHEMA.md. Write
   harvest/fulltext/login-rejudge-report.md: extracted / re-judged / how
   many judgments CHANGED (function or centrality). The flip rate decides
   whether a larger login fetch happens.
2. [sonnet] **verified-quirks**: fix the two data quirks the impact-view
   lane flagged in harvest/repos/verified.json — duplicate URL spellings
   (normalize + dedupe) and own-group rows misroled third_party (correct
   role). Coordinate with the impact-view lane's client-side filters (it
   can drop them once the data is clean). Rebuild affected data/ outputs.
3. [sonnet] **S1 ecosystems — STATUS UNKNOWN, RESUME**: no
   data/repos/*.json and no harvest/ecosystems/candidates-report.md on
   main. Check ~/workspace/nextgen for uncommitted ecosystem work and
   commit it; otherwise (re)run the tier-2 harvests for
   tensor-compiler/taco, exaloop/codon(+seq), bthies/streamit per the
   round-4 spec (code search on usage signatures, metrics, evidence, Batch
   curation with the SDV integration vocabulary, under-$20 rule). Then the
   MEASURE step: ecosystem-size report for every own-group repo to
   harvest/ecosystems/candidates-report.md and STOP for the coordinator's
   gating.
4. [sonnet] **S2 descendants-all — STATUS UNKNOWN, RESUME**: no
   harvest/repos/descendants.json on main. Same drill: commit any local
   work, else run tier-3 extraction for ALL papers from the classification
   data (extends/adopts-idea at core/engaged centrality -> locate the
   citing work's repo; paper-only when none found; no guessing).
5. [sonnet] **halide-import — verify**: confirm the Halide-world import for
   halide:pldi:2013 landed (tier-2/3 rows sourced from
   samanamarasinghe/Halide-world with attribution); if not, do it per the
   round-4 spec.

## PARKED (needs the human)
- Authors sitting #2: harvest/authors/session-sheet.md is ready.
- Larger login fetch: gated on task 1's flip rate.
- Ecosystem gating beyond the 3 pilots: gated on task 3's report
  (coordinator decides).

## DONE (recent)
- ALL summaries live (157 receptions per style doc; pilots + hand-written
  text untouched); impact view live for all papers (131 with repo panels,
  162 repos + 23 artifacts); citation feature complete; 23 login PDFs
  fetched by the human; wave QA: 16 texts fixed by verifier agents
