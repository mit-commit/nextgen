# Task queue — maintained by the coordinator (chat Claude)

Sessions: `git pull`, claim in docs/LANES.md, execute, log, push, re-read this
file. **Do not edit this file** — the coordinator owns it.

## STANDING RULES
Batches under $20 auto-submit; wave-then-checkpoint; his role is only spot
checks, GUI review, account-gated fetching. Canonical-over-fork for repos.

## HUMAN RULINGS 2026-08-26 (author links) — unchanged, see git history for
the full text: LinkedIn default; permanent academic page replaces it; else
best active site; else email. Identity must be POSITIVELY verified
(education/timeframe for MIT students; employer/era for others);
unconfirmables go to HIM with evidence, never dropped, never guessed.

## OPEN — round 11 (2026-08-26)
1. [sonnet] **UI combinatorial test run** (human-requested): build
   tests/ui/facet_test.py — an oracle + driver that PROVES the site
   displays the right papers. (a) ORACLE: compute expected paper sets
   from the data files directly (publications.json + data/citations/* +
   data/repos/*) for facet selections; (b) DRIVER: serve the site
   locally, drive it headless (playwright, pip install playwright +
   chromium on the worker machine), apply selections, read the rendered
   paper list + header counts; (c) COVERAGE: every single-facet value
   (each author, category, year, venue, has-repo/has-citations toggles),
   all pairwise facet-type combos (sampled ~200), ~100 random 3-4-way
   combos, each x the 3 sort modes and the Show summaries/citations/
   repositories toggles on a sample; verify counts, membership, sort
   order, and header labels. (d) Report tests run / failures with repro
   selections in tests/ui/report.md. Display-only fixes may be made
   directly; anything structural STOPs for Fable + coordinator.
2. [fable, after 1] **UI test failure review**: judge failures, fix or
   escalate; re-run the harness to green; leave the harness in the repo
   as a pre-cutover gate (document in docs/refresh.md).
3. [sonnet] **carried from round 10 if unclaimed**: authors fold +
   OpenAlex backstop (task 1), thesis-mining fold when the verify batch
   returns, rejudge round 2, site round leftovers (TEMP button, halide
   tier-3 162 fold, spot-render).

## PARKED (needs the human / coordinator)
- LinkedIn sittings: IN PROGRESS — coordinator driving his Chrome over
  the ~326 non-academic people, starting with the 74 who have no link at
  all; results land in harvest/authors/linkedin-results.json per sitting.
- Fable site task 3 (author links on pages) waits for those sittings.
- Cutover: final review, then point the live site at nextgen.

## DONE (recent)
- Author-links free harvest: links.json all 369 — 43 permanent-academic,
  237 professional, 3 personal, 12 email-only, 74 nothing.
- Authors Scholar sitting: session-results.json (31 match / 2 tentative /
  35 no-profile / 9 backstop).
- Round 9: sitting-2+3 ingest complete, miss audit, thesis-mining scan
  (verify batch in flight), deephunt closed.
