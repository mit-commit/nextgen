# Task queue — maintained by the coordinator (chat Claude)

Sessions: `git pull`, pick the first OPEN task whose paths are unclaimed in
docs/LANES.md, claim it there, do it, log completion in docs/LANES.md, push,
then re-read this file for the next task. **Do not edit this file** — the
coordinator owns it.

## HUMAN RULINGS IN FORCE (2026-08-24/25)
- THREE-TIER repo model per paper: (1) own repo, (2) users of that repo
  (SDV-world style: api_user / derivative_work / inherited / fork /
  name_collision...), (3) idea descendants (repos of citing works classified
  extends/adopts-idea at high centrality). Same repo on multiple papers is
  expected and fine.
- Tier-2 pilot ecosystems APPROVED: tensor-compiler/taco, exaloop/codon,
  bthies/streamit. Halide PLDI'13 imports from the existing Halide-world
  index (samanamarasinghe/Halide-world) — never re-harvest it.
- The TACO paper (Kjolstad:2017:TTG:3155562.3155683) JOINS the pilot set
  (now 9) — add it to the "Pilot papers only [TEMP]" button.
- Repo verification rule: prefer the CANONICAL repo over forks (search
  returned manya-bansal/taco where tensor-compiler/taco is canonical).
- Summaries: reception merges into Summary; repo/artifact get one natural
  sentence inside it. AWAITING his critique of the 9 pilot summaries before
  scaling prose.
- Batches under $20 submit without asking; above, stop for approval.

## OPEN
1. [sonnet] **repo-verify** (claims harvest/repos/verified*): model pass over
   ALL papers' repo evidence — the in-paper mentions (own vs third-party) and
   the 52 high + 165 medium search candidates. For each paper emit
   repos: [{url, role: implementation|artifact|benchmark|third_party,
   own_group: bool, confidence, evidence}]. Canonical-over-fork rule. Reject
   candidates whose evidence is name-similarity alone. Output
   harvest/repos/verified.json; genuinely ambiguous rows to
   harvest/repos/review.json (human spot-check). Pilot papers first, push
   after the pilots, then continue.
2. [sonnet] **pilot-button**: add Kjolstad:2017:TTG:3155562.3155683 to the
   TEMP pilot filter (9 papers). One-line change; any session.
3. [sonnet] **repo-ecosystems** (claims harvest/ecosystems/): tier-2 harvest
   for the 3 pilot ecosystems (taco, codon+seq, streamit). Port the SDV
   pattern: GitHub code search on import/usage signatures (taco: '#include
   "taco' / pytaco; codon: import codon / seq; streamit: str files / streamit
   imports), bisect by file size past the 1000-cap, exclude forks of the
   canonical; then metrics (stars, commits, contributors); then a light
   evidence fetch (README + hit context). Curate via Batch API with the SDV
   integration vocabulary (under-$20 rule). Output
   harvest/ecosystems/<repo>/ + merged
   data/repos/<ecosystem>.json. Report per ecosystem: repos found /
   curated / by integration type.
4. [sonnet] **halide-import** (claims harvest/ecosystems/halide*): import
   the Halide-world index for halide:pldi:2013 — read
   samanamarasinghe/Halide-world data (built site JSON / shards) and map its
   repo nodes + roles onto our tier-2/tier-3 schema; do not re-harvest or
   re-judge. Attribute the source in the data file.
5. [sonnet] **idea-descendants** (claims harvest/repos/descendants*): for
   the 9 pilot papers, take citing works classified extends or adopts-idea
   with centrality core/engaged, find each one's code repo (from its
   abstract/S2 record/fulltext where cached; light web check), and emit
   tier-3 descendant edges with evidence. No guessing — a descendant with no
   located repo is recorded as paper-only.

## DONE (recent)
- GS counts merged (display = max(verified, gscholar)); citation UI +
  sorts + cross-paper finder live; corpus classified (11,082);
  reception→Summary merge done for pilots (awaiting critique);
  repos-search step 2 done (52 high / 165 medium / 27 low / 24 none)
