# Task queue — maintained by the coordinator (chat Claude)

Sessions: `git pull`, claim in docs/LANES.md, execute, log, push, re-read this
file. **Do not edit this file** — the coordinator owns it.

## STANDING RULES
Batches under $20 auto-submit; wave-then-checkpoint; his role is only spot
checks, GUI review, account-gated fetching. Canonical-over-fork for repos.

## RULING (coordinator, 2026-08-25): "own repo" means THIS paper's artifact.
A same-lab/group repo that isn't the paper's own artifact is not own_group
for that paper — it may still appear via dependency/third-party routes.
Apply to the 4 disagreement rows in deephunt_review_resolved.json: reject
as own_group. (Human said trust the audit trail; this closes the lane.)

## OPEN — round 9 (2026-08-25)
1. [sonnet] **fulltext ingest + miss audit**: sitting #2 is complete on
   his machine. (a) Ingest ~/workspace/nextgen-fulltext into the repo
   fulltext staging exactly as sitting #1 (text extraction, per-paper
   files, no PDFs committed if that was the rule — follow sitting #1's
   route in harvest/fulltext/). (b) AUDIT the misses: diff
   login-worklist2.json against files on disk + the three _run-log*.json;
   classify every miss (rate-limited-unattempted, 403, 404, 200-HTML
   access wall, session-dead casualties). (c) For each miss, VERIFY the
   row: does the DOI resolve at Crossref? right publisher? was our URL
   pattern wrong (like the stamp.jsp bug)? (d) For misses with a FREE
   alternate route (arXiv/OpenAlex OA url), fetch directly — no login
   needed. (e) Emit harvest/fulltext/login-worklist3.json with ONLY
   genuinely recoverable login-gated rows (correct/repaired URLs), and a
   miss report (counts per class) in harvest/fulltext/sitting2-report.md.
   STOP after: coordinator decides if a third sitting is worth his time.
2. [sonnet, after 1] **rejudge round 2**: re-run the low-confidence
   re-judge over every row whose evidence tier improved via sitting #2
   ingest (same route as the round-6 rejudge; batch under-$20 rule
   applies per wave, report flip rates).
3. [fable] **site round**: (a) spot-render the big new panels (halide,
   dynamorio, taco) — tier bars, sorts, counts, responsiveness with
   573-row Halide; fix display-only, flag structural. (b) DELETE the
   TEMP "Pilot papers only" button + its plumbing (human-approved).
   (c) Fold Halide's 162 staged tier-3 rows into data/repos/ (same
   attribution rule as the 567; coordinator-approved).
4. [sonnet] **deephunt closure**: apply the RULING above to the 4 rows,
   merge/log, close deephunt_review lane.

## PARKED (needs the human)
- Possible login sitting #3: gated on task 1's report.
- Authors sitting #2: harvest/authors/session-sheet.md is ready.
- Cutover: final review, then point the live site at nextgen.

## DONE (recent)
- Round 8: all three repo tiers live corpus-wide (162 papers / 2,024
  repo rows), Halide 573 rows, own-inventory folded (29 papers gained
  first repo), confidence pass (8 rejections confirmed, 1 reversal).
- Login sitting #2 fetching complete on his machine: ACM 1000+ /1432
  (rate-limit tail), IEEE run finished, Springer 936/966 (28 access
  walls + 2 dead DOIs).
